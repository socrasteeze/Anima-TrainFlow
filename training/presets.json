{
  "presets": [
    {
      "name": "Style — AdamW8bit (safe)",
      "optimizer": "AdamW8bit",
      "learning_rate": "0.00008",
      "network_rank": 16,
      "training_steps": 1000,
      "train_batch_size": 4,
      "gradient_accumulation_steps": 1,
      "save_steps": 250,
      "sample_steps": 250
    },
    {
      "name": "Style — AdamW8bit (low VRAM)",
      "optimizer": "AdamW8bit",
      "learning_rate": "0.00008",
      "network_rank": 16,
      "training_steps": 1000,
      "train_batch_size": 2,
      "gradient_accumulation_steps": 2,
      "save_steps": 250,
      "sample_steps": 250
    },
    {
      "name": "Prodigy (default / auto-LR)",
      "optimizer": "Prodigy",
      "learning_rate": "1.0",
      "network_rank": 16,
      "training_steps": 1000,
      "train_batch_size": 4,
      "gradient_accumulation_steps": 1,
      "save_steps": 250,
      "sample_steps": 250
    },
    {
      "name": "Style — Prodigy — batch 4",
      "optimizer": "Prodigy",
      "learning_rate": "1.0",
      "network_rank": 16,
      "training_steps": 600,
      "train_batch_size": 4,
      "gradient_accumulation_steps": 1,
      "save_steps": 100,
      "sample_steps": 100
    },
    {
      "name": "Style — Prodigy — batch 2",
      "optimizer": "Prodigy",
      "learning_rate": "1.0",
      "network_rank": 16,
      "training_steps": 1200,
      "train_batch_size": 2,
      "gradient_accumulation_steps": 1,
      "save_steps": 150,
      "sample_steps": 150
    },
    {
      "name": "Style — AdamW8bit — batch 4",
      "optimizer": "AdamW8bit",
      "learning_rate": "0.00008",
      "network_rank": 16,
      "training_steps": 600,
      "train_batch_size": 4,
      "gradient_accumulation_steps": 1,
      "save_steps": 100,
      "sample_steps": 100
    },
    {
      "name": "Style — AdamW8bit — batch 2",
      "optimizer": "AdamW8bit",
      "learning_rate": "0.00006",
      "network_rank": 16,
      "training_steps": 1200,
      "train_batch_size": 2,
      "gradient_accumulation_steps": 1,
      "save_steps": 150,
      "sample_steps": 150
    }
  ]
}
